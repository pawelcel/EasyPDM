# Skopiuj ten plik do .env i ustaw prawdziwe hasło przed "docker compose up".
#   cp .env.example .env
PDM_DB_PASSWORD=zmien-to-haslo

# Opcjonalnie: port hosta, pod którym dostępny jest EasyPDM (domyślnie 5000). Zmień, jeśli
# port 5000 jest już zajęty przez coś innego na tej maszynie.
# PDM_HOST_PORT=8081
